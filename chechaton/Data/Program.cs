using System;
using System.Collections.Generic;
using System.Collections.ObjectModel;

namespace Data
{
    //class Program
    //{
    //    static void Main(string[] args)
    //    {
    //        DayManager dayManager = new DayManager("json.json");
    //        for (int i = 1; i <= 12; i++)
    //        {
    //            ObservableCollection<Day> block = dayManager.GetCalendary(i, DateTime.Today.Year);
    //            Console.WriteLine(i);
    //            block.ForEach(element => Console.WriteLine(element.Date.ToString() + " " + element.Date.DayOfWeek ));
    //        }
    //        dayManager.taskManager.WriteAll();
    //    }
    //}
}
